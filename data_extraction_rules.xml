package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Eye-Soothing Soft Medical & Pharmacy Palette
val TealPrimary = Color(0xFF0D9488)        // Soft Medical Teal
val TealPrimaryLight = Color(0xFF14B8A6)   // Gentle Light Teal Accent
val TealDark = Color(0xFF115E59)           // Rich Dark Teal
val TealContainer = Color(0xFFCCFBF1)      // Soothing Light Mint/Teal Background Container
val OnTealContainer = Color(0xFF0F766E)    // Deep Teal Text on Container

val SecondaryBlue = Color(0xFF0EA5E9)      // Soft Sky Blue Accent
val SecondaryContainer = Color(0xFFE0F2FE) // Gentle Soft Sky Blue Container

val GoldAccent = Color(0xFFD97706)         // Warm Gold Accent
val GoldContainer = Color(0xFFFEF3C7)      // Soft Amber Container

val DarkBackground = Color(0xFF111827)     // Eye-safe Dark Slate Background
val DarkSurface = Color(0xFF1F2937)        // Dark Card Surface
val DarkSurfaceVariant = Color(0xFF374151) // Dark Secondary Surface

val LightBackground = Color(0xFFF8FAF9)    // Eye-soothing Soft Canvas (Off-white / Soft Sage-Gray tint)
val LightSurface = Color(0xFFFFFFFF)       // Crisp White Card Surface
val LightSurfaceVariant = Color(0xFFF1F5F9) // Soft Grey-Blue Variant Container

val CardBorder = Color(0xFFE2E8F0)         // Subtle Soft Card Border
val SuccessGreen = Color(0xFF10B981)       // Soft Emerald Success Green


